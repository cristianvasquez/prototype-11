// @ts-ignore
// import {DateTimeFormatOptions} from "luxon";

const PLUGIN_NAME = 'prototype-11'

const dateTimeFormat = {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
  hour: 'numeric',
  minute: '2-digit',
}

const THIS = '__THIS__'
const NOT_FOUND = '__NOT_FOUND__'

export { PLUGIN_NAME, dateTimeFormat, THIS, NOT_FOUND }